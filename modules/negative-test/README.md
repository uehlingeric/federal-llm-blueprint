# Negative Test

Deliberately non-compliant module proving each CI gate fails for its own reason. Never merged.

VIOLATION (docs gate): the section between the markers below is stale — `make docs` regenerates it differently, so `docs-check` fails.

<!-- BEGIN_TF_DOCS -->
## Inputs

No inputs.
<!-- END_TF_DOCS -->
